import type { NextPage } from 'next'
import Head from 'next/head'
import Image from 'next/image'
import Header from '../components/Header'

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Home - Lisa Movie App</title>
        <meta name="description" content="Lisa Movie App" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Header></Header>
      <main>
        {/* Banner */}
        <section>{/* Row */}</section>
      </main>

      {/* Modal */}
    </div>
  )
}

export default Home
